use rand::prelude::*;
mod input;
mod expression;
pub mod simplification;
use input::*;

fn main() {
    // let seed: u64 = get_number("Zadej seed");
    // let mut _rnd = StdRng::seed_from_u64(seed);
    // for _idx in 0.. {
    // }

    println!("x²");
}
